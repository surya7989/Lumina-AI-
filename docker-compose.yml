services:
  app:
    build: .
    container_name: ai-learning-proxy
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5000
      CLIENT_URL: http://localhost:5000
      GROQ_API_KEY: ${GROQ_API_KEY:-}
